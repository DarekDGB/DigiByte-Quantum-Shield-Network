name: DQSN Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Placeholder tests (always pass)
        run: echo "DQSN tests placeholder – full test suite coming in v2"
